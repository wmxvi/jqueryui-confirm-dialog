# jqueryui-confirm-dialog
Quick and dirty jquery ui confirmation dialog

Usage: 

    $('a.my-link').confirm({
        'title': 'Confirmation',
        'msg': msg,
        'url': $('a.my-link').attr('href')
    });
